btc_direction:
  paper_trading: true
  initial_paper_balance: 500.0       # USDC simulados

  # ── Riesgo ──
  risk:
    max_trade_usdc: 20.0             # Máximo USDC por trade
    max_open_positions: 2            # Máximo posiciones abiertas simultáneas
    momentum_threshold_pct: 0.15     # Retorno mínimo BTC 5m para generar señal (%)
    min_price_edge: 0.03             # Edge mínimo: 0.5 - precio_polymarket

  # ── Datos de BTC ──
  btc_data:
    exchange: kraken
    pair: BTC/USDT
    timeframe: 1m
    lookback_candles: 10             # Últimas 10 velas 1m para calcular momentum
    fallback_exchange: okx

  # ── Polymarket ──
  polymarket:
    gamma_api: "https://gamma-api.polymarket.com"
    clob_api: "https://clob.polymarket.com"
    series_slug: "btc-up-or-down-15m"
    slot_duration_seconds: 900       # Cada slot dura 15 minutos
    min_remaining_seconds: 60        # No entrar si quedan menos de 60s en el slot
    max_entry_seconds: 600           # No entrar si ya pasaron más de 10m del slot
